import { Component } from '@angular/core';
import { RouterOutlet } from '@angular/router';

@Component({
  selector: 'app-entity-models-page',
  template: `<router-outlet />`,
  imports: [RouterOutlet],
})
export class EntityModelsPage {}
