import { Component } from '@angular/core';
import { RouterOutlet } from '@angular/router';

@Component({
  selector: 'app-action-themes-page',
  template: `<router-outlet />`,
  imports: [RouterOutlet],
})
export class ActionThemesPage {}
