import { Component } from '@angular/core';
import { RouterOutlet } from '@angular/router';

@Component({
  selector: 'app-action-models-page',
  template: `<router-outlet />`,
  imports: [RouterOutlet],
})
export class ActionModelsPage {}
