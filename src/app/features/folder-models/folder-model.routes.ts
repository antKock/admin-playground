import { Routes } from '@angular/router';

import { FolderModelListComponent } from './folder-model-list.component';

export const folderModelRoutes: Routes = [
  { path: '', component: FolderModelListComponent },
];
